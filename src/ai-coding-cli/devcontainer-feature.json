{
  "id": "ai-coding-cli",
  "version": "0.2.0",
  "name": "AI Coding CLIs (Claude Code, Codex, OpenCode)",
  "description": "Installs common AI coding CLIs as an optional add-on.",
  "options": {
    "nodeMajor": {
      "type": "string",
      "default": "22",
      "description": "Node major version to install if node is missing"
    },
    "installZsh": {
      "type": "boolean",
      "default": true,
      "description": "Install zsh and set it as default shell for vscode user"
    },
    "installZshrc": {
      "type": "boolean",
      "default": true,
      "description": "Install a minimal zshrc into the remote user's home"
    },
    "overwriteZshrc": {
      "type": "boolean",
      "default": false,
      "description": "Overwrite existing ~/.zshrc if present"
    }
  }
}
